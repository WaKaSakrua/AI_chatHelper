package com.bdq.android.data.repository;

/**
 * 数据仓库层包
 * Repository �?ViewModel 和数据层之间的中间层
 * 负责决定数据从本地（Room）还是远程（API）获�? * 后续会添�?ChatRepository
 */
public final class RepositoryPackageInfo {
    // 占位，后续添�?Repository
}
